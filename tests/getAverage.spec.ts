import { expect } from "chai";
const sinon = require("sinon");

describe("1. Get Average Function Test Suite", function () {
  after(() => {
    console.log("====================***=====================");
  });
  before(() => {
    console.log("====================***=====================");
  });

  beforeEach(() => {
    // sinon.restore();
    console.log("--------------------------------------------");
  });
});
